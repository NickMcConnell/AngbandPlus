## Makefile.am -- Process this file with automake to process Makefile.in

faangband_files = \
  delete.me

EXTRA_DIST = \
  $(faangband_files)

if SET_GID
faangbanddir = @DEFAULT_PATH@user

faangband_DATA = \
  $(faangband_files)
endif

