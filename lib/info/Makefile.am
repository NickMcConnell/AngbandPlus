## Makefile.am -- Process this file with automake to process Makefile.in

angband_files = \
  README.txt \
  announce \
  posband.vim \
  randart-changelog.txt \
  wizard.txt

EXTRA_DIST = $(angband_files)

angbanddir = $(datadir)/games/$(PACKAGE)/info

angband_DATA = $(angband_files)
